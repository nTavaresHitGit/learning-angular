import { FirstTypePipe } from './first-type.pipe';

describe('FirstTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FirstTypePipe();
    expect(pipe).toBeTruthy();
  });
});
